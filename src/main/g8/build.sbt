name := "$name$"

version := "$version$"

organization := "$package$"

scalaVersion := "$scalaVersion$"

resolvers += "AFABL Repository" at "http://repo.afabl.org"

libraryDependencies += "org.afabl" %% "afabl" % "$afablVersion$"
